// Keep HTTP and WebSocket roots in one place so deployment URLs can be changed
// without hunting through component logic.
export const API_BASE = 'http://127.0.0.1:8000';
export const WS_BASE = 'ws://127.0.0.1:8000';
